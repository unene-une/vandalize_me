# お絵かき練習アプリ 仕様書

## 概要

毎日のお絵かき練習を継続させるためのWebアプリ。ランダムなお題を提示し、ブラウザ上でそのまま描いて、練習記録を保存・閲覧できる。

- **技術スタック**: HTML + CSS + JavaScript（単一ファイル推奨）
- **データ保存**: `localStorage`（サーバー不要、ブラウザ完結）
- **外部API**: 使用しない

---

## 画面構成

アプリは3つのタブ／ビューで構成する。

1. **今日の練習**（メイン画面）
2. **カレンダー**（進捗一覧）
3. **ギャラリー**（サムネイル一覧）

---

## 1. 今日の練習（メイン画面）

### お題エリア

- 画面上部に本日のお題を大きく表示する
  - 例：「猫」「古い建物」「ラーメン」
- **「お題を変える」ボタン**: 別のお題にランダム再抽選する
- お題は以下のジャンルをすべてミックスしてランダム選出する

#### お題リスト（ジャンル別）

| ジャンル | 例 |
|---|---|
| キャラクター・人物 | 笑っている人、走っている子供、魔法使い、ロボット、老人の顔 |
| 動物 | 猫、犬、うさぎ、鳥、魚、クマ、ペンギン |
| 食べ物 | ラーメン、ケーキ、りんご、寿司、パン、コーヒーカップ |
| 風景・建物 | 古い建物、橋、夕日、海、森、駅、街並み |

- お題は最低 **50個以上** をハードコードして内包する
- 同じセッション内での重複は避ける（シャッフル方式）

---

### キャンバスエリア

#### キャンバスサイズ

- デフォルト: **800 × 600px**
- サイズ変更オプション（プリセット選択）:
  - 600 × 600（正方形）
  - 800 × 600（横長・デフォルト）
  - 600 × 800（縦長）
- サイズ変更時は **キャンバスの内容をリセット**（変更前に確認ダイアログを出す）

#### ツールバー（キャンバス上部または左側に配置）

| ツール | 詳細 |
|---|---|
| ブラシ | 通常の描画ツール |
| 消しゴム | 背景色（白）で描画 |
| ブラシサイズ | スライダーで 1px 〜 50px |
| 色選択 | カスタムカラーピッカー（下記仕様参照）|
| Undo | 直前の操作を取り消す（最大 **20ステップ** まで履歴保持） |
| 全消し | キャンバスを白紙に戻す（確認ダイアログあり） |

#### カラーピッカー詳細仕様

ツールバーの色選択をクリックすると、フローティングパネルとして表示する。3つのタブで切り替える。

**共通**
- 現在の選択色を大きなプレビューボックスで常時表示する
- HEXコード入力欄を設け、直接入力でも色を指定できる
- パネル外クリックで閉じる

---

**タブ1: カラーサークル**

- 色相環（Hue wheel）を円形で表示する
- 中央に**彩度・明度の2Dグラデーション正方形**（左→右で彩度、上→下で明度）を配置する
- ドラッグで色相・彩度・明度をそれぞれ直感的に選択できる
- Canvas API または SVG で自前実装する（外部ライブラリ不使用）

**タブ2: HSBスライダー**

| スライダー | 範囲 | 説明 |
|---|---|---|
| H（色相） | 0 〜 360 | レインボーグラデーション背景 |
| S（彩度） | 0 〜 100% | グレー→選択色グラデーション背景 |
| B（明度） | 0 〜 100% | 黒→選択色グラデーション背景 |

- 各スライダーの背景はリアルタイムでグラデーション更新する
- 数値入力欄も併設する（直接入力可）

**タブ3: RGBスライダー**

| スライダー | 範囲 |
|---|---|
| R（赤） | 0 〜 255 |
| G（緑） | 0 〜 255 |
| B（青） | 0 〜 255 |

- 各スライダーの背景は対応チャンネルのグラデーションを表示する
- 数値入力欄も併設する（直接入力可）

---

**プリセットカラー**

3つのタブ共通で、パネル下部に固定表示する。

- 黒・白・赤・青・緑・黄・茶・灰 の8色を小さなスウォッチで並べる
- クリックで即座に選択色に反映する

---

**タブ間の連動**

- どのタブで色を変更しても、他のタブの値もリアルタイムで同期する
  - カラーサークルの位置 ↔ HSB値 ↔ RGB値 ↔ HEXコード がすべて連動

---

#### 保存・ダウンロード

- **「今日の1枚として保存」ボタン**: キャンバスの内容をPNGとしてlocalStorageに保存し、今日の日付に紐づける
  - すでに今日の記録がある場合は「上書きしますか？」と確認する
- **「画像をダウンロード」ボタン**: PNGファイルとしてローカルに保存（`oekaki_YYYY-MM-DD.png` という命名）

---

## 2. カレンダービュー

- 月単位のカレンダーを表示する
- 練習済みの日付は **色付きのドット** または **背景色の変化** で示す
- 各日付をクリックすると、その日に保存した絵と使用したお題をポップアップ表示する
- 月の切り替えボタン（前月 / 次月）を設ける
- 練習日数のサマリーを表示する（例：「今月 12日 練習しました」）

---

## 3. ギャラリービュー

- 保存した絵をサムネイル形式で新しい順に一覧表示する
- 各サムネイルに日付とお題を表示する
- サムネイルをクリックすると拡大表示する
- **削除ボタン**: 個別の記録を削除できる（確認ダイアログあり）

---

## データ設計（localStorage）

### キー設計

```
oekaki_record_YYYY-MM-DD  → JSON オブジェクト（1日1レコード）
```

### レコード構造

```json
{
  "date": "2025-06-01",
  "theme": "猫",
  "imageData": "data:image/png;base64,...",
  "savedAt": "2025-06-01T20:34:00"
}
```

---

## UX・その他要件

- **レスポンシブ対応は不要**（PCブラウザ専用で可）
- **タッチ操作（スマホ・タブレット）への対応は任意**
- キャンバスはマウスドラッグで描画する（`mousedown` / `mousemove` / `mouseup` イベント）
- アプリ全体を **単一HTMLファイル**（`index.html`）にまとめる
- 外部ライブラリは使用しない（Vanilla JS + Canvas API のみ）
- デザインはシンプルで清潔感のあるスタイル

---

## 将来的な拡張候補（今回のスコープ外）

- タイムラプス機能（描画過程の録画）
- お題のカスタム追加
- 練習時間の計測・記録
- SNSシェア機能

---

## スマホ・タブレットアプリ化ロードマップ

### ステップ1: タッチ・レスポンシブ対応

現在のHTMLに以下を追加する。

- タッチイベント対応: `mousedown/mousemove/mouseup` に加えて `touchstart/touchmove/touchend` を実装する
- Apple Pencil・スタイラス筆圧対応: `PointerEvent` API を使用する
- ピンチズームでキャンバスの拡大縮小ができるようにする
- ツールバーのボタンサイズをタップしやすいサイズに調整する（最小 44×44px 推奨）
- 画面サイズに応じたレスポンシブレイアウトに対応する

### ステップ2: データ保存の強化

- `localStorage`（上限5MB前後）から **IndexedDB** に移行する
- 画像データが増えても容量不足にならない

### ステップ3: PWA化（推奨）

2ファイルを追加するだけでホーム画面へのインストール・オフライン動作が可能になる。App Storeへの申請は不要。

| 追加ファイル | 役割 |
|---|---|
| `manifest.json` | アプリ名・アイコン・テーマカラーなどの定義 |
| `service-worker.js` | オフライン動作・キャッシュ管理 |

### ステップ4: ストア配信（任意）

**Capacitor**（推奨）を使ってHTMLをネイティブアプリの殻に入れ、App Store / Google Play に配信する。ビルド環境として Node.js と Xcode（iOS）または Android Studio（Android）が必要になる。
